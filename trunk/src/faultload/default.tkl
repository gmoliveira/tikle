#---------------------------------
# Tikle script example
#---------------------------------

@declare {127.0.0.1};

start:
  after (5s) do
    partition {127.0.0.1};
  end
  after (10s) do
    partition {127.0.0.1};
  end
stop:
  after (15s);
